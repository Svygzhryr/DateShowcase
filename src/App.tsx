import React from 'react';
import styles from './styles/App.module.scss'

const App: React.FC = () => {
    return (
            <h1 className={styles.title}>Hello React + TypeScript + SCSS Modules!</h1>
    );
};

export default App;